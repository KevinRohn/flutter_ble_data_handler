name: flutter_ble_data_handler
description: A flutter package to handle bluetooth low energy data transmission.
version: 0.1.1
author: kevin@rohn.io

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: 3.0.0
  crclib: 3.0.0
  rxdart: ^0.26.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true